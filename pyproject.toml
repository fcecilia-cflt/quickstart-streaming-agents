[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "quickstart-streaming-agents"
version = "0.2.0"
description = "Confluent Streaming Agents Quickstart. Real-time data meets Agentic AI."
readme = "README.md"
authors = [
    {name = "Confluent", email = "support@confluent.io"},
    {name = "Brenner Heintz", email = "bheintz@confluent.io"}
]
license = {text = "Apache-2.0"}
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Education",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: Apache Software License",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Database",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Distributed Computing",
]
keywords = [
    "confluent", "kafka", "flink", "streaming", "agents", "vector-search",
    "ai", "mcp", "inference", "embeddings", "bedrock", "openai",
    "claude", "rag", "azure-ai", "llm", "agentic-framework", "agentic-ai"
]
requires-python = ">=3.10"

# Core dependencies
dependencies = [
    "PyYAML>=6.0.1,<7.0",
    "python-dotenv>=1.0.0,<2.0",
    "urllib3>=1.26.18,<2.0", # Pin to v1.x for LibreSSL compatibility on macOS
    "pymongo>=4.10.1",
    "boto3>=1.26.0,<2.0", # Required for workshop-keys command
]

# Optional dependencies for enhanced functionality
[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "black>=23.0.0",
    "isort>=5.12.0",
    "flake8>=6.0.0",
    "mypy>=1.0.0",
    "detect-secrets>=1.5.0",
    "pre-commit>=3.5.0",
]
mongodb = [
    "pymongo>=4.0.0,<5.0",
]
elasticsearch = [
    "elasticsearch>=8.0.0,<9.0",
]


[project.scripts]
deploy = "deploy:main"
destroy = "scripts.common.destroy:main"
#
lab1_datagen = "scripts.lab1_datagen:main"
lab1-datagen = "scripts.lab1_datagen:main"
#
lab3_datagen = "scripts.lab3_datagen:main"
lab3-datagen = "scripts.lab3_datagen:main"
#
publish_docs = "scripts.publish_docs:main"
publish-docs = "scripts.publish_docs:main"
#
clear_mongodb = "scripts.common.clear_mongodb:main"
clear-mongodb = "scripts.common.clear_mongodb:main"
#
clear_elasticsearch = "scripts.common.clear_elasticsearch:main"
clear-elasticsearch = "scripts.common.clear_elasticsearch:main"
#
lab2_publish_queries = "scripts.lab2_publish_queries:main"
publish_queries = "scripts.lab2_publish_queries:main"
publish-queries = "scripts.lab2_publish_queries:main"
#
generate_deployment_summary = "scripts.common.generate_deployment_summary:main"
generate-deployment-summary = "scripts.common.generate_deployment_summary:main"
deployment_summary = "scripts.common.generate_deployment_summary:main"
deployment-summary = "scripts.common.generate_deployment_summary:main"
#
workshop_keys = "scripts.common.workshop_key_manager:main"
workshop-keys = "scripts.common.workshop_key_manager:main"
#
generate_summaries = "scripts.common.generate_lab_flink_summary:main"
generate-summaries = "scripts.common.generate_lab_flink_summary:main"
#
validate = "scripts.common.validate:main"


[project.urls]
Homepage = "https://github.com/confluentinc/quickstart-streaming-agents"
Repository = "https://github.com/confluentinc/quickstart-streaming-agents"
Documentation = "https://github.com/confluentinc/quickstart-streaming-agents#readme"
Issues = "https://github.com/confluentinc/quickstart-streaming-agents/issues"

# Hatchling build configuration
[tool.hatch.build.targets.wheel]
packages = ["scripts"]

[tool.hatch.build.targets.sdist]
include = [
    "scripts/",
    "README.md",
    "LICENSE",
]
exclude = [
    "/.git",
    "/.github",
    "/aws",
    "/azure",
    "/terraform",
    "/assets",
    "*.pyc",
    "__pycache__",
]

# Development tools configuration
[tool.black]
line-length = 88
target-version = ['py38']
include = '\.pyi?$'
extend-exclude = '''
/(
    \.git
  | \.venv
  | \.env
  | venv
  | env
  | __pycache__
  | \.pytest_cache
)/
'''

[tool.isort]
profile = "black"
line_length = 88
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true

[tool.mypy]
python_version = "3.8"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--verbose",
    "--cov=scripts",
    "--cov-report=term-missing",
    "--cov-report=html",
]